const fs = require('fs');
const path = require('path');

const renderTeam = path.resolve(TeamMember) => {
    const createManager = (manager) => {
        
    };
    replacePlaceholders
    path.resolve(templatesDir, "main.html"),
    "utf8"


};